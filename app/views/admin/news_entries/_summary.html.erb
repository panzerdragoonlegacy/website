<div class="form-group">
  <%= f.label :summary, 'Summary' %>
  <%= f.text_area :summary, rows: 3, class: 'form-control' %>
  <%= render(
    partial: 'admin/shared/form/info',
    locals: {
      info: 'A short summary of the news. Limit the summary to one sentence ' \
        'and reuse it on social media.'
    }
  ) %>
</div>
