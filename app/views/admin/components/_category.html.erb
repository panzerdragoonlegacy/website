<div class="form-group">
  <%= f.label :category_id, 'Category*' %>
  <%=
    f.collection_select(
      :category_id,
      categories,
      :id,
      :name,
      { include_blank: true },
      { class: 'form-control' }
    )
  %>
</div>
