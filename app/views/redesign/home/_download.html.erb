<article class="home-download">
  <div class="home-download__picture">
    <%= link_to(
      show_picture(download, 'download_picture', :original),
      redesign_download_path(download)
    ) %>
  </div>
  <h1 class="home-download__heading">
    <%= link_to download.name, redesign_download_path(download) %>
  </h1>
  <footer class="home-download__more">
    <%= link_to(
      'More Downloads',
      redesign_contributions_path(media_type: 'downloads')
    ) %>
  </footer>
</article>
