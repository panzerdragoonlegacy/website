<% unless tagged_count(tag) == 0 %>
  <div class="tagged-content">
    <h1 class="tagged-content__heading">Tagged Content</h1>
    <ul class="tagged-content__list">
      <% unless news_entry_count(tag) == 0 %>
        <li class="tagged-content__list-item">
          <%= link_to(
            pluralize(news_entry_count(@tag), 'news entry'),
            redesign_tag_news_entries_path(@tag)
          ) %>
        </li>
      <% end %>
      <% unless literature_count(tag) == 0 %>
        <li class="tagged-content__list-item">
          <%= link_to(
            pluralize(literature_count(tag), 'work') + ' of literature',
            redesign_tag_literature_index_path(tag)
          ) %>
        </li>
      <% end %>
      <% unless picture_count(tag) == 0 %>
        <li class="tagged-content__list-item">
          <%= link_to(
            pluralize(picture_count(tag), 'picture'),
            redesign_tag_pictures_path(tag)
          ) %>
        </li>
      <% end %>
      <% unless music_track_count(tag) == 0 %>
        <li class="tagged-content__list-item">
          <%= link_to(
            pluralize(music_track_count(tag), 'music track'),
            redesign_tag_music_tracks_path(tag)
          ) %>
        </li>
      <% end %>
      <% unless video_count(tag) == 0 %>
        <li class="tagged-content__list-item">
          <%= link_to(
            pluralize(video_count(tag), 'video'),
            redesign_tag_videos_path(tag)
          ) %>
        </li>
      <% end %>
      <% unless download_count(tag) == 0 %>
        <li class="tagged-content__list-item">
        <%= link_to(
          pluralize(download_count(tag), 'download'),
          redesign_tag_downloads_path(tag)
        ) %>
      </li>
      <% unless quiz_count(tag) == 0 %>
        <li class="tagged-content__list-item">
          <%= link_to(
            pluralize(quiz_count(tag), 'quiz'),
            redesign_tag_quizzes_path(tag)
          ) %>
        </li>
      <% end %>
    <% end %>
  </ul>
<% end %>
