version: '3'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/cms
      - paperclip_data:/cms/public/system
    env_file:
      - .env
  database:
    image: postgres
    env_file:
      - .env
    volumes:
      - database_data:/var/lib/postgresql/data
volumes:
  database_data:
  paperclip_data:
